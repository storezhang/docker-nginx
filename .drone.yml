kind: pipeline
name: docker-nginx

steps:
  - name: submodules
    image: docker:git
    commands:
      - git --version
      - git pull --recurse-submodules
      - ls modules/nginx-dav-ext-module


  - name: docker
    image: plugins/docker
    pull: always
    when:
      event:
        - pull_request
        - tag
    settings:
      repo: storezhang/nginx
      dockerfile: ./Dockerfile
      mirror: https://docker.mirrors.ustc.edu.cn
      username:
        from_secret: docker_username
      password:
        from_secret: docker_password
      auto_tag: true
      force_tag: true
